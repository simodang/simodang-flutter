import 'package:flutter/material.dart';

class PondDeviceStep extends StatelessWidget {
  const PondDeviceStep({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Perangkat Kolam'),
      ],
    );
  }
}
